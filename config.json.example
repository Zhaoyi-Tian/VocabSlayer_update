{
  "_comment": "数据库配置文件 - 可以在这里切换不同的数据库",

  "database_type": "excel",

  "database_config": {
    "data_dir": "server"
  },

  "_opengauss_config_example": {
    "database_type": "opengauss",
    "database_config": {
      "host": "192.168.1.100",
      "port": 5432,
      "database": "vocabulary_db",
      "user": "opengauss_user",
      "password": "your_password"
    }
  },

  "_ssh_tunnel_config_example": {
    "database_type": "opengauss",
    "use_ssh_tunnel": true,
    "ssh_config": {
      "ssh_host": "remote_server_ip",
      "ssh_port": 22,
      "ssh_user": "ssh_username",
      "ssh_password": "ssh_password"
    },
    "database_config": {
      "host": "localhost",
      "port": 5432,
      "database": "vocabulary_db",
      "user": "db_user",
      "password": "db_password"
    }
  }
}
